\# default: on
# description: The Time Zone Server
service tz-server
{
        flags           = REUSE
        socket_type     = stream
        wait            = no
        user            = nobody
        server          = /usr/local/bin/tz-server
        disable         = no
}

